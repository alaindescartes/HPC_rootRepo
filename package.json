{
  "name": "hpc_rootrepo",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-client": "cd client && npm install",
    "install-server": "cd server_node/server && npm install",
    "build-client": "cd client && npm run build",
    "heroku-postbuild": "ls -al && ls -al server_node && npm run install-client && npm run install-server && npm run build-client",
    "start": "node server_node/server/server.js"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
